{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/course && npm run build && cp -r dist ../../part3/backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push origin main"
  },
  "author": "Olivier",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^18.0.0",
    "express": "^5.2.1",
    "mongoose": "^9.9.4"
  }
}
